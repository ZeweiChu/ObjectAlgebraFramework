package _syb;

public class TypeCheck {
	/*
	 * Check types in conditions and computed questions.
	 * The only boilerplate we avoid here is traversing lists of questions and
	 * descending into bodies of conditional questions. 
	 * 
	 * Monoid is set of errors.
	 */
}
